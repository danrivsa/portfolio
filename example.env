# Base URL of the portfolio agent server. Must include the /api prefix.
# NEXT_PUBLIC_* vars are inlined into the static export at build time, so this
# must be set on the build host (CI/deploy), not just locally.
# Copy this file to .env.local (gitignored) for local development.
NEXT_PUBLIC_AGENT_SERVER_URL=http://localhost:8000/api
